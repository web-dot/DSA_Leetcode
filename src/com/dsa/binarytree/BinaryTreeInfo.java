package com.dsa.binarytree;

public class BinaryTreeInfo {
	
	/*
	 * 1. i can search a `tree` quickly, as i can a ordered array.
	 * 2. i insert, delete items quickly as i can with a linked list.
	 * 
	 * 					binary search tree(bst):
	 * 					========================
	 * a nodes left child must have a key less than its parent, and nodes right
	 * child must have a key greater than or equal to its parent.
	 * 
	 * */
}
